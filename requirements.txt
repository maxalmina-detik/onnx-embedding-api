fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1

llama-index-core==0.12.10
llama-index-embeddings-huggingface==0.4.0

# sentence-transformers>=3.2 is required for backend="onnx"/"openvino" support
sentence-transformers==3.3.1

# transformers==4.46.3 (pulled in transitively) doesn't recognize the
# "gemma3_text" architecture used by onnx-community/embeddinggemma-300m-ONNX,
# which raises a ValueError during model loading and crashes the app before
# it can bind to $PORT — Railway's healthcheck then times out waiting for a
# server that never started. transformers>=4.50.0 adds gemma3_text support.
# Upper-bounded to stay inside optimum[onnxruntime]==1.26.1's supported range
# (see the optimum pin below) — bump both together if you upgrade either.
transformers>=4.50.0,<4.53.0

# CPU-only torch wheel — Railway has no GPU, and sentence-transformers still
# imports torch even when the actual embedding inference runs via ONNX Runtime.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1

# ONNX Runtime CPU build + Optimum, used internally by the sentence-transformers
# "onnx" backend. If deploying on a GPU host instead, swap this line for
# onnxruntime-gpu and set DEVICE=cuda / ONNX_PROVIDER=CUDAExecutionProvider
# in your environment (see README "Running with a GPU").
optimum[onnxruntime]==1.26.1
onnxruntime==1.20.1